doctype html
html(lang="en")
head
title SmartDiary
meta(name="viewport" content="width=device-width, initial-scale=1.0")
link(rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css")
script(src="https://stackpath.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js")
link(rel="stylesheet" href="/style.css")
script(src="../speech.sdk.js")
script(src="../recognition.js")
script(src="../index.js")

body(class="bgr-img-logged")
    nav.navbar.navbar-expand-lg.navbar-light.bg-light
        a.navbar-brand(href='#') SmartDiary
        button.navbar-toggler(type='button', data-toggle='collapse', data-target='#navbarSupportedContent', aria-controls='navbarSupportedContent', aria-expanded='false', aria-label='Toggle navigation')
            span.navbar-toggler-icon
        #navbarSupportedContent.collapse.navbar-collapse
            ul.navbar-nav.mr-auto
                li.nav-item.active
                    a.nav-link(href='/')
                        | Add memory
                        span.sr-only (current)
                li.nav-item
                    a.nav-link(href='/memories') My memories

    fieldset#memories
        if memories.length
            - each memory in memories
                .memory
                    div( class="w3-container w3-light-grey w3-text-brown")
                        h5 #{memory.title} - #{memory.date}
                        p #{memory.memory}
        else
            .nomemory There are no memories!


